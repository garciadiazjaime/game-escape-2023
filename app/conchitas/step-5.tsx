import { StepProps } from "@/support/types";

export default function Home({ status }: StepProps) {
  if (!status) {
    return <div>blanco como azahar, tiene raíces pero no es árbol</div>;
  }

  return (
    <div>
      <div>mm xxxxx j</div>
    </div>
  );
}
